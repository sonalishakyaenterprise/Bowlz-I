/**
 * POST /api/contact
 * Phase 1 stub — activate in Phase 2 on Vercel
 */

export const dynamic = "force-static";

export async function GET() {
  return Response.json({ message: "Contact API — activate in Phase 2 on Vercel" });
}
